dist
coverage
playwright-report
test-results
.lighthouseci
pnpm-lock.yaml
# harness:agnostic
.codex/*.toml
# /harness:agnostic

# Layer A is generated from the `harness` repo and verified by sha. Reformatting it here
# would break that check on every sync, and the fix belongs upstream anyway: the plugin
# owns its own formatting.
.agents/vendor/

# A run's worktree is a full second copy of this repository; every ignore above is
# defeated one level down inside it. See eslint.config.js for the long version.
.factory/
